import React from 'react';
import EventListitem from "./EventListItem";

const EventList = () => {
    return (
        <div>
           <EventListitem/>
           <EventListitem/>
           <EventListitem/>
           <EventListitem/>
        </div>
    );
};

export default EventList;
